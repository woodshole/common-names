#colLeft
  .box#header
    %h1#logo
      = image_tag 'gbif_small.gif', :alt => 'GBIF'
      Common Names for Higher Taxa
.box.clear

#flash
  = render :partial => "/shared/flash", :locals => { :flash => flash }