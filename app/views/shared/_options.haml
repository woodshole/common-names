#options
  %h2.colored
    Options
  - if current_user
    %p
      Current Language:
      - if @language == nil
        All
      - else
        =h @language 
    %ul
      %li
        = link_to 'view all taxa', root_path
      %li
        - if current_user
          = link_to 'filter based on my language preference', language_path(current_user.language.iso_code)
      %li
        Select Other
        - options = Language.all.map!{|d|[d.english_name, d.iso_code]}
        = select_tag 'choose_language', options_for_select(options.unshift(['all','']))
  - else
    %p 
      You must be logged in to see your options!